 
 // The Object.keys() static method returns an array of a given object's own enumerable string-keyed property names.

   // Syntax  => Object.keys(obj)
 
    // obj  (is an parameter)

  // for Example 
  
  const Obj1 = {
    a: "Name",
    b: 1234,
    c: true
 
  }

  console.log(Object.keys(Obj1))   // Output is new Array =['a','b','c'] 

